package com.travel.cardbooking.employee.api

import play.api.libs.json._

trait ServiceMessages {

  //  case class Employee(id: String, name: String, email: String, approver: Boolean, active: Boolean)
  //  object Employee { implicit val format: Format[Employee] = Json.format }
  //
  //  case class Employees(employees: List[Employee])
  //  object Employees { implicit val format: Format[Employees] = Json.format }
  //
  //  case class OptionEmployee(oe: Option[Employee])
  //  object OptionEmployee { implicit val format: Format[OptionEmployee] = Json.format }
}
